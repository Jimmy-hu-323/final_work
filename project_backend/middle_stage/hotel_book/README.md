# Hotel Book

面向酒店、普通用户和 AI Agent 的酒店库存与预订平台。

当前目录处于规划阶段，完整产品范围、API、数据模型、手机端集成方式、测试策略和迭代顺序见 [PLAN.md](./PLAN.md)。

项目包含统一的可视化账单中心：用户可集中查看、调整和支付账单；AI 默认没有付款权限，只能在用户签发指定账单、限额、限时的一次性授权后代理付款。

推荐从 `PLAN.md` 的“开工前必须确认的产品决策”和“第一批开发任务”开始。
