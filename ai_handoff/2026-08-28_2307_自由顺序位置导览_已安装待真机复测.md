# 自由顺序位置导览：已构建安装，待真机复测

时间：2026-08-28 23:07（Asia/Shanghai）

## 用户最新需求与最高优先级边界

用户明确修正旧需求：游客不一定按行程走；点击开始旅程后持续定位，到达相应景点就讲对应故事，**不按计划日期或路线先后限制**。

本轮向用户说明的实现范围：识别当前已开始行程里全部有坐标的景点，可跨计划日期、乱序和返回前站；**没有增加行程外陌生景点的自动搜索/发现**。如果用户下一步要求任何行程外景点也能自动讲解，需要在旅程/对话必要范围内另做附近景点识别，不要误称本版已支持全部澳门 POI。

继续遵守：

- 项目根 `Z:\qwen_compitition\final_work`，权威源码在 `project_backend`，对应文件才同步 `project_front`。
- App：**LensGo 澳门旅游助手（本地版）**，包名 `io.lensgo.macao.mobile.local`。
- **只处理旅程/对话必要逻辑，不修改其他栏目；不得改变现有前端布局、样式、导航和展示组件。**
- **不要提交 Git。** HEAD 仍为 `cd400ae` / `v3.0.1`，暂存区为空。
- 保留原有未提交修改；不读取/输出 Token、密钥、真实聊天正文或私密 workspace。

本记录替代此前记录中“必须满足行程日期和景点顺序”的产品规则。旧记录保留历史事实，不覆盖。

## 上一轮只读诊断的结论

用户手动点击电脑定位控制台后，手机系统确实收到模拟位置，App 曾显示“已到达议事亭前地 · 误差约 5 米”。未讲解不是 USB 没连上。

只读 UI 核验发现，所选行程的议事亭前地为第 2 天第 7 站，大三巴为第 2 天第 9 站，当前下一站并非两者；旧导览规则只识别当前自然日及下一未讲解站，所以阻止了触发。没有导出完整用户行程、聊天或真实坐标。

## 本轮改动

仅以下源码文件及对应前端镜像：

1. `project_backend/middle_stage/Qwen_competion/qwen_compitition/console/src/mobile-local/tripGuide.ts`
   - `TripArrivalTracker.observe` 不再用计划 day、startedAt 日历或 currentStopIndex 过滤候选景点。
   - 检查当前 active 行程全部坐标有效的 stops，保留原触发半径（100–200m，默认120m）和精度要求。
   - 选择范围内距离最近的景点，再检查是否已讲解；已讲解的最近景点不会导致站在原地继续串讲周围重叠范围的景点。
   - 保留 20 秒连续停留、过期/低精度/重复样本过滤、离开和切换候选时重置停留。
   - 移除无其他调用的 `guideTripDay`。
   - 没有修改原 visit key 格式或清空已有防重复记录。
2. 同目录 `tripGuide.test.ts`
   - 将原“日期/顺序限制”测试改为跨天、乱序、返回早先站点的正向测试。
   - 新增最近重叠景点选择、已访问不串讲、非 active 不触发和切换景点重新停留测试。
   - runtime 测试使用计划为第 2 天且索引在当前进度之前的景点，验证独立流式故事、旧聊天保留、重挂载防重及结束停定位。
3. `project_front/.../src/mobile-local/tripGuide.ts` 和 `tripGuide.test.ts`
   - 仅同步上述两个文件，SHA256 与权威源一致。
4. `location_demo/README.md`
   - 更新新版 APK 的说明：不限制日期/顺序，景点须在当前行程已收录的坐标集合内，保留停留及防重复。
   - 控制台 HTML/CSS/JS/backend 未改，手机页面也未改。
5. 本交接文件。

`tripGuideRuntime.ts`、`MobileLocalApp.tsx`、原生桥、API、权限、配置、所有样式和页面组件本轮均未修改。运行时原有 currentStopIndex 更新和下一站展示逻辑仍保持原样，不把本轮描述成重做路线进度/UI。

## 构建、测试与范围核验

- 权威 console：`node_modules/.bin/vitest.cmd run src/mobile-local`，**4 个文件、18 项全部通过**。
- `scripts/build_android.ps1 -DebugBuild`，退出0，最终 Gradle `BUILD SUCCESSFUL`。
  - TypeScript/mobile Vite 构建成功。
  - Windows symlink 权限仍有限制；使用项目已有脚本内 `.so` 复制 + Gradle fallback，未修改系统开发者模式/安全策略或构建脚本。
  - 保留现存 SDK XML/Gradle deprecation 警告，不是本次错误。
- `audit-guide-scope.mjs` 只读 AST 审计通过：其他栏目函数、导航和样式与原基线一致。
  - 初次受限环境启动 Git 子进程遇 EPERM；批准的正常权限重跑通过。
- 本轮前后对根仓库已有16个 tracked dirty文件做 SHA256比较，全部不变；本次变动集中在上一轮新增的两个 untracked导览文件及其镜像。
- `git diff --check` 通过，仅现存 CRLF 提示；未暂存、未提交。

## 手机 APK 与回退产物

新 APK：

`project_backend/middle_stage/Qwen_competion/qwen_compitition/console/src-tauri/gen/android/app/build/outputs/apk/arm64/debug/app-arm64-debug.apk`

SHA256：`BA4888A06DAAA238A34559367073770799401400840B44CE7D5EFD54AA79DB8E`

已用 `adb install -r` **保留数据覆盖安装成功**，手机已安装 base.apk 的 SHA256 与上述值相同。未卸载、未清数据、未改/删原行程和旧聊天。不能据此声称所有运行数据做过字节级一致性核验；本轮刻意没有读取真实聊天或导出私密存储。

安装前旧 APK 另备份到本机任务工作目录（未加入项目/Git）：

`C:\Users\28757\Documents\Codex\2026-08-28\z-qwen-compitition-final-work\work\before-free-order-guide.apk`

旧版是之前 `8347126E5833AAB4C8226C36A7649A5BDD1F6D5A0D694EE0954895C4947FFFFE` 那版，可用于需要时保留数据回退安装；未自动回退。

## 真机检查事实与未完成项

1. 用户在本轮开始前已自行通过控制台开启模拟、定位到议事亭前地。安装时保留该现有位置，没有自行切新坐标。
2. 新版安装后打开 App。手机曾 Dozing，检查无安全锁后仅唤醒屏幕，再进入“旅程”。没有改系统休眠配置。
3. 页面正常显示，原行程仍为“进行中”；取消了遮挡页面的既有人流提示（“不用，继续”），**没有点击按客流调整或重排路线**。
4. 本轮未看到新的景点故事，**不能宣称新版跨天/乱序真机端到端已通过**。
5. 控制台在约23:03记录“定位发送中断，停止发送并保留恢复记录”，随后“手机已重新连接，自动恢复真实定位”。原因未进一步定性；不要把日志中的恢复理由字符串当成确实发生物理拔插的证据。
6. 最终控制台 `active=false`、`needsRestore=false`，shell mock 权限 `default`，模拟已恢复。没有为了补测擅自重新开启模拟。
7. 用户此前授权模拟测试，但网页切换依浏览器操作规范需临操作确认，本轮已异步问是否允许通过控制台切换大三巴/议事亭并收尾恢复，尚未收到明确回复。没有换工具来绕过此确认。

后续收到确认后：保持手机亮屏、App在旅程/对话；在已有未讲解景点或隔离演示行程上，通过控制台模拟跨天/乱序位置并停留至少30秒，核对新对话/正文/选项及重复保护，再恢复真实定位。不要清空生产去重集合来强行复测，也不要用 unit mock 的成功替代手机证据。

## 运行服务与其他状态

原人流18099、酒店18110、Qwen18088仍正常；这些服务的健康/API只读检查返回200。LensGo 18000/18765 未改动、未停止。新定位控制台18120也保持运行但不模拟。

手机业务 reverse 18088/18099 保留；本轮没有新建 WebView 调试 forward。临时 `/data/local/tmp/lensgo-free-guide-*.xml` UI层级诊断文件已逐个删除；未删除用户文件。

App旅程仍保持用户原本的 active 状态，因此恢复模拟后它会按用户此前授权继续接收真实定位；没有擅自结束用户行程。用户不再使用时应在App结束旅程。
