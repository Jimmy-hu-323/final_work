# 导览真实附近推荐验收与全部后端启动

时间：2026-08-28 22:12（Asia/Shanghai）

本文件接续 `2026-08-28_2135_旅程定位自动导览实现与真机验证_待地图授权.md`。该旧记录中的“待地图授权/真实检索未验收”现已解决；旧记录保留作为当时状态。

## 必须继续遵守

- 项目：`Z:\qwen_compitition\final_work`，权威源码在 `project_backend\middle_stage\Qwen_competion`。
- App：**LensGo 澳门旅游助手（本地版）**，包名 `io.lensgo.macao.mobile.local`。
- **只改旅程和对话相关必要逻辑，不改现有前端展示，不改其他栏目、全局样式或导航。**
- **不要提交 Git。** 当前 HEAD 仍为 `cd400ae` / `v3.0.1`，暂存区为空，前一轮和本轮源码改动仍未提交。
- 不展示、输出或提交密钥/Token/真实聊天正文/私密 workspace。不得整树同步前端镜像覆盖既有差异。
- 用户本轮明确允许“导览接口在服务器内部复用人流服务已有高德密钥，不修改人流配置、不向手机暴露密钥”，并追加要求启动所有后端。该授权不允许更改人流业务或配置。

## 本轮代码改动

只修改权威后端的两个代码/测试文件，没有改前端、原生代码、APK、人流/酒店源码或配置：

1. `qwen_compitition/src/qwenpaw/app/routers/travel_planner.py`
   - 增加仅供 `POST /api/travel-planner/guide/nearby` 调用的 `_guide_amap_key()`。
   - 优先保留原 `_amap_key()` 返回的显式导览配置；否则使用服务器环境或人流项目 `.env` 中的 `AMAP_WEB_KEY` / `AMAP_KEY`。
   - 人流根路径遵循已有 `LENSGO_CROWD_PROJECT_ROOT`；相对路径相对完整 Qwen 项目根解析；未配置时采用同级 `data_publish`。
   - 只提取两个地图字段，不 import 人流配置、不向 `os.environ` 注入整份配置、不写 `.env`；原 `_amap_key()` 和其他既有接口不变。
   - 密钥只用于服务器访问高德，响应只包含规范化 POI；保留已有错误脱敏和 urllib 请求，避免日志输出含 key 的 URL。
2. `tests/test_trip_guide.py`
   - 更新 mock 目标为导览专用 resolver，避免缺配置测试读取真实配置。
   - 新增显式配置优先、不污染进程环境、环境值优先、共享文件不可读等测试；合计 **8 项全部通过**。

本轮没有前端变动，因此不需要再打 APK 或同步 `project_front`。手机仍使用上一轮已核对哈希的最终修正版 APK。

## 真实检索与真机结果

服务器使用大三巴测试坐标请求真实高德接口，餐饮和拍照景点各返回 5 条，`available=true`。随后在 Type-C 手机使用 Android **network 系统模拟位置源**：从景点外开始行程，移动到大三巴并连续停留，自动建立新的导览会话。

在手机原有“对话”页面实际点击“附近美食”和“拍照打卡点”：

- 两类结果均显示成功，各含 5 个地点、地图评分、距离、地址、类别和位置链接。
- 查询前刷新系统模拟位置，结果明确以“手机最新位置”为原点，并非过期位置或硬编码前端结果。
- 餐饮评分实测为 4.8、4.6、4.6、4.6、4.6；例如南屏雅叙返回 4.6、约 296m。地点和分数来自接口，没有编造。
- 拍照候选评分实测为 4.5、4.3、4.3、4.2、4.0；例如澳门关前正街返回 4.5、约 170m。
- 拍照类别是地图景点候选，界面明确说明景点评分不等于出片效果评分；具体拍摄限制需遵守现场规定。本轮不是逐个地点实地摄影质量认证。
- 没改任何布局、颜色、样式或导航。截图已查看，结果沿用现有聊天消息排版。

测试工具需使用每轮唯一行程 ID（现在为 `qa-guide-` + 时间戳）。最初误用了上一轮测试 ID，运行中的防重复集合正确阻止重讲；随后结束并清理旧测试，使用新 ID 完成验收，没有清空生产去重集合或修改触发规则。

### 清理与核验边界

- 已结束并移除本轮临时行程及本轮新建的 1 个测试导览会话。最终新 ID 测试前后的 3 个旧聊天与 10 份旧行程核验相等，清理返回 `originalTripsRestored=true`、`originalChatsRestored=true`。
- 最初重复旧 ID 那轮，清理工具曾报告一次原聊天原始 JSON 全等为 false（当时未生成新导览会话）。未保存该轮原始聊天正文，也未进一步读取用户正文排查，因此不能将最后一轮的通过表述为所有中间快照均字节一致。
- 已移除 network 测试 provider，shell `android:mock_location` 恢复 default；真实定位开关 true。结束时 LensGo 当前定位订阅 0、活跃模拟 provider 0。
- 已移除调试 forward tcp:9222。
- **为满足用户启动全部后端并继续使用手机 App 的要求，保留业务 adb reverse tcp:18088 与 tcp:18099。** 这与上一轮全部清理端口不同，是有意保留的可用连接；拔插 USB 后可按原启动方式重建。
- 未遍历或删除 QwenPaw 私密 workspace；服务器可能留有本次 Agent 测试会话/日志，不宣称服务端痕迹已全部删除。

## 全部项目内后端已启动并保持运行

沿用根目录 `02_本地启动指南.md` 的三个完整模块。未初始化/覆盖数据库，未运行 bootstrap，未修改业务配置。既有人流保留，酒店与 LensGo 新启动；仅重启此前由本任务启动的 QwenPaw 进程组以加载导览代码。

| 服务 | 端口 | 22:10 最终进程 PID | 验证 |
| --- | --- | --- | --- |
| 人流 data_publish | 18099 | 23300 | `/api/health` HTTP 200 |
| 酒店 Vinext/API | 18110 | 24076 | `/`、`/api/v1/openapi`、`/api/v1/state` 均 HTTP 200 |
| QwenPaw/Console | 18088 | 6824 | `/api/version` HTTP 200，导览真实检索成功 |
| LensGo HTTP/Bridge | 18000 | 19176 | 使用私有鉴权检查 `/api/bridge/status`，status=ok、QwenPaw reachable=true；Token 未输出 |
| LensGo WebSocket | 18765 | 19176 | 监听正常，使用项目现有服务进程 |

后台启动均使用隐藏窗口。QwenPaw 启动器 PID 8888，LensGo 启动器 PID 25416，酒店启动器 PID 4292。PID 仅是本次状态，后续停止/重启前应重新核实归属，不能只凭旧 PID 杀进程。

项目文档中的可选 AI Drive / 外部 AMap MCP 不属于当前已部署的三个核心后端模块；本次没有安装额外服务、插件或修改其配置。

## 最终验证

- 导览 Python unittest：8/8 通过。
- 后端 AST 审计：基线所有既有函数不变；新增仅为之前的四个导览函数加本轮 `_guide_amap_key`。
- 前端 AST、导航及样式审计：无其他栏目差异。
- `git diff --check` 通过；只有原有行尾转换提示。
- 人流、酒店目录无代码差异；没有提交或暂存 Git。
- 上一轮 15 项前端测试、APK 构建、后台定位边界和旧静态测试过期说明仍见前一份记录，本轮未声称重跑所有前端测试或验证被杀进程后的长时定位。

本次脚本和脱敏测试截图：

`C:\Users\28757\Documents\Codex\2026-08-28\z-qwen-compitition-final-work\work`

- `guide-live-food.png`
- `guide-live-photo.png`

同目录的后端 stdout/stderr 日志可能包含运行时私密信息，不要展示或提交。
