# 旅程界面地图与按天路线实机验证交接

## 用户目标与范围

- 按用户手绘稿重构手机本地版的“旅程”界面。
- 保持 LensGo 现有橙色、米白、白色卡片风格，不采用草图中的蓝色标注色。
- 只允许改变“旅程”页面；底部导航、其他栏目和全局主题不得改变。
- 完成后安装到通过 Type-C 连接的 Android 手机，并在“LensGo 澳门旅游助手（本地版）”中实机验证。

## 修改文件

以下两处前后端镜像已保持相同 SHA-256，内容一致：

- `project_backend/middle_stage/Qwen_competion/qwen_compitition/console/src/mobile-local/MobileLocalApp.tsx`
- `project_backend/middle_stage/Qwen_competion/qwen_compitition/console/src/mobile-local/mobileLocal.module.less`
- `project_front/middle_stage/Qwen_competion/qwen_compitition/console/src/mobile-local/MobileLocalApp.tsx`
- `project_front/middle_stage/Qwen_competion/qwen_compitition/console/src/mobile-local/mobileLocal.module.less`

本次只修改上述文件中的 `LocalTravelPage` 及 `journey*` 专属样式：

- 顶部保留“本地旅行规划”，右侧增加橙色圆形开始按钮；活动行程时变为结束按钮。
- 将原来的纵向行程列表改为单一“行程选择”下拉框，显示状态、路线点数和日期。
- 新增横向“总览 / 第 N 天”选择；切换日期后地图和下方详情同时过滤到对应日期。
- 地图卡片支持总路线或当天路线，并保留现有高德底图、路线绘制、客流更新和离线说明能力。
- 地图下方新增对应范围的行程详情卡片，显示站点顺序、时间、备注和可用客流信息。
- 原“生成并保存行程”表单没有删除，收进“规划新行程”弹窗，避免占据主视图。
- 完整 Markdown 规划说明在“总览”下折叠展示。
- 未修改底部导航组件、其他页面 JSX 或共享主题变量。

## 构建与测试

- `npm run build:mobile`：通过；Vite 共转换 3343 个模块。
- `npx vitest run src/mobile-local`：通过；3 个测试文件、5 个测试全部通过。
- `build_android.ps1 -DebugBuild`：通过。
  - Windows 不允许创建 Tauri JNI 符号链接，脚本按既有回退逻辑复制 ARM64 库并执行 Gradle。
  - 最终 `:app:assembleArm64Debug` 构建成功。
- APK：`project_backend/middle_stage/Qwen_competion/qwen_compitition/console/src-tauri/gen/android/app/build/outputs/apk/arm64/debug/app-arm64-debug.apk`
- APK SHA-256：`23DAA115B9F053767B75A55CBA777C8C2215B0840FB6DC90C826C69079943EC6`

## Type-C 手机实机验证

- 设备：Samsung `SM-A5360`，ADB 序列号 `R5CT530CV4B`，连接状态 `device`。
- 应用包：`io.lensgo.macao.mobile.local`，版本 `0.0.1`（versionCode 1）。
- 使用 `adb install -r` 覆盖安装成功，保留了手机本地数据。
- 已验证：
  - “旅程”页维持现有橙色/米白/白色视觉体系，底部导航未变化；
  - 行程下拉框能展开已有 9 份本地行程并切换，标题、路线点数、日期、地图和详情随之更新；
  - 4 天行程能显示“总览、第1天、第2天、第3天、第4天”；
  - 选择“第1天”后地图只显示当天 5 个路线点；
  - 从地图外区域向上滑动可以进入“第1天行程”详情，站点时间和备注正确显示；
  - 右上圆形开始按钮能弹出“确认开始这份行程”定位说明。
- 实机测试在开始确认框选择了“暂不开始”，未改变任何行程的开始/结束状态。
- 按当前应用进程过滤运行日志，未发现崩溃、JavaScript TypeError/ReferenceError 或未捕获异常；仅有 Android WebView 自身的 variations seed 签名提示，与业务无关。
- 应用当前仍安装在手机上并可直接使用；ADB reverse 保留 `18088`、`18099` 两条映射。

## 当前服务与端口

本次没有停止或重启既有服务，检查时以下端口仍监听：

- `127.0.0.1:18000`：LensGo Bridge；
- `127.0.0.1:18088`：QwenPaw，本次检查 `/health` 返回 200；
- `127.0.0.1:18099`：客流服务，存在两个既有监听进程；
- `127.0.0.1:18110`：酒店服务；
- `127.0.0.1:18765`：LensGo WebSocket。

## 风险、未解决项与下一步

- 没有本次功能阻塞项。
- Leaflet 地图区域内的纵向手势会用于拖动地图；要向下查看详情，应从日期按钮、卡片边缘或地图下方说明区域开始上滑。这是当前地图的正常交互。
- 根工作区在本次开始前已有账单、运行时和既有交接文件等未提交改动；本次全部保留，未回滚也未覆盖。
- 若继续修改旅程页，应同步修改 `project_backend` 和 `project_front` 两份镜像，并再次执行移动构建和实机验证。

## Git 状态

- 根仓库：分支 `main`，基线提交 `69598c9`。
- Qwen_competion 子仓库：分支 `codex/lensgo-agent-cloud-sync`，基线提交 `51e592e`。
- 本次未创建 Git 提交或标签。
