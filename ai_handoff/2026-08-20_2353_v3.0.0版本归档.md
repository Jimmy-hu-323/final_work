# v3.0.0 版本归档交接

## 用户目标

- 将 `Z:\qwen_compitition\final_work` 保存为 Git 版本 `v3.0.0`。
- 明确根目录三份长期说明文档与 `ai_handoff` 增量交接记录的职责。
- 在 `01_项目架构与文件说明.md` 中补充 `ai_handoff` 使用方法。

## 本次工作

- 核验实际根路径为 `Z:\qwen_compitition\final_work`；用户消息中带额外反斜杠的路径不存在。
- 将架构文档中的目录树修正为“三份编号文档位于根目录”。
- 新增 `ai_handoff\README.md`，规定命名、必填内容、只新增不覆盖和敏感信息边界。
- 建立根级 Git 安全忽略规则，排除密钥、数据库、运行工作区、依赖、构建产物、日志、备份和归档。
- 保留 `data_publish`、`hotel_book`、`Qwen_competion` 三个子仓库自己的 `.git` 历史，不执行 reset、clean 或删除。

## 此前完成的本地版手机验证

- 验证对象：`io.lensgo.macao.mobile.local`（LensGo 澳门旅游助手·本地版）。
- QwenPaw `18088`、人流 `18099`、酒店 `18110` 健康检查返回 HTTP 200；LensGo `18000/18765` 正常监听。
- Type-C/ADB reverse 保持 `18088/18099/18000/18765` 映射。
- 本地行程、路线图、账单、相册和设置页可读取；酒店服务补启动后账单页由 502 恢复正常。
- 未发送新聊天、未生成图片、未新增行程、未上传照片、未付款。

## 已知问题

- 本地版聊天失败时可能留下空白助手气泡；流式占位消息在异常路径没有删除。
- 模型输出中的 HTML 注释可能被原样显示。
- 首页“本地运行时在线”是硬编码展示，“模型 API 已配置”只代表配置字段存在。
- 直连模型请求没有明确 HTTP 超时，连接测试可能长时间等待。
- Android 本地版 `versionName` 仍为 `0.0.1`，与本次仓库版本标签不是同一概念。

## Git

- 根快照版本：`v3.0.0`。
- 版本只包含源码、说明和必要静态资源，不包含任何真实密钥或运行数据。
