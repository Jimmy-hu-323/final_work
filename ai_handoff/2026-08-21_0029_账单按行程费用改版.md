# 账单按行程费用改版交接

## 用户目标与范围

- 只修改 LensGo 澳门旅游助手（本地版）的“账单”栏目。
- 图片仅作为信息结构参考，视觉样式继续沿用现有 App。
- 账单改为“选择旅行行程 → 查看该行程预算总计 → 查看并增删改费用明细”；费用变化后总额自动重算。
- 未修改底部导航、栏目顺序，以及 LensGo、对话、旅程、相册、设置页面。

## 修改内容

- `project_backend/middle_stage/Qwen_competion/qwen_compitition/console/src/mobile-local/HotelBillsPage.tsx`
  - 账单标题与说明改为行程预算语义。
  - 增加本地行程选择器、预算总计、必要/可选支出和费用明细。
  - 增加费用新增、编辑、删除弹窗与操作入口。
- `project_backend/middle_stage/Qwen_competion/qwen_compitition/console/src/mobile-local/runtime.ts`
  - 接入已有行程费用查询、创建、更新、删除接口及类型。
- `project_backend/middle_stage/Qwen_competion/qwen_compitition/console/src-tauri/src/mobile_runtime.rs`
  - 为 Android/Tauri 账单网关增加行程费用四类操作。
- `project_backend/middle_stage/Qwen_competion/qwen_compitition/console/src/mobile-local/mobileLocal.module.less`
  - 仅增加账单专用样式，复用现有颜色、卡片、圆角、间距和阴影体系。
- 新增 `HotelBillsPage.test.tsx`，并将四个前端账单文件同步到 `project_front` 对应镜像。

## 验证结果

- `npm run build:mobile`：通过，3343 个模块完成构建。
- `vitest run src/mobile-local/HotelBillsPage.test.tsx`：1 个测试通过。
- `rustfmt --edition 2021 --check src/mobile_runtime.rs`：通过。
- 只读请求 `GET /api/travel-planner/trip-expenses?trip_id=codex-readonly-check`：返回空明细、总额 0、币种 CNY，接口正常。
- 412 × 915 手机视口检查：账单页继续沿用现有 App 风格，底部导航保持不变。
- `cargo check --lib`：未完成；本机缺少 MSVC `link.exe`，在依赖构建阶段终止，未发现与本次文件有关的编译诊断。

## 当前状态与注意事项

- 根版本基线仍为 `v3.0.0`（提交 `69598c99e3d3`）；本次改动尚未另行提交或打标签。
- 行程费用后端和 QwenPaw 代理接口在本次任务前已经存在，本次没有修改酒店服务、旅程生成或其他业务栏目。
- 实机使用时需要 QwenPaw、hotel_book 后端正常运行；选择的本地行程 ID 用于筛选对应费用。
