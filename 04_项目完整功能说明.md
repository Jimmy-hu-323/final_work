# LensGo 澳门旅游助手：App 与后端网页完整功能说明

> 本文依据当前仓库中的手机端源码、QwenPaw 控制台路由、业务后端页面、API 实现和测试整理。目标是列出当前版本已经实现或已经保留的全部主要功能，并明确哪些能力依赖外部服务、哪些数据仅用于演示。

## 1. 产品总览

LensGo 是一套以澳门旅行场景为核心的多端智能旅游系统。它不是单一 App，而是由以下部分共同组成：

1. **LensGo Android 本地 App**：面向游客，提供智能对话、旅行规划、实时定位导览、账单预算、本地相册、照片讲解、姿势效果图和隐私控制。
2. **QwenPaw 智能体服务**：承载主 Agent、子 Agent、Skills、MCP、模型调用、会话和旅行规划工具。
3. **QwenPaw 桌面/网页控制台**：面向开发、配置和运营人员，管理智能体、模型、工具、会话、定时任务、安全策略等。
4. **人流与巴士模拟数据发布器**：面向数据发布人员，发布景点人数、拥挤度和模拟巴士报站数据。
5. **酒店预订与账单系统**：面向游客和酒店运营人员，展示酒店搜索、报价、订单、账单、付款授权和库存工作台。
6. **LensGo 设备服务**：接入智能眼镜的文字、语音、图片、视频、TTS、事件和媒体上传链路。

核心数据流如下：

```text
智能眼镜 / Android App / 桌面网页
                │
                ▼
        LensGo 与 QwenPaw 服务
       ├─ 主 Agent 与子 Agent
       ├─ 旅行规划、地图、导览
       ├─ 图片理解与图片生成
       ├─ 酒店、费用与账单
       └─ 人流、巴士与实时提醒
                │
                ▼
      手机通知、TTS、地图、相册
```

## 2. Android App 的全部主要功能

手机 App 使用底部导航，当前共有 6 个入口：**LensGo、对话、旅程、账单、相册、设置**。

### 2.1 LensGo 本地中心

本地中心是 App 首页，集中展示手机端状态并提供两个高频入口。

#### 2.1.1 状态看板

- 显示手机本地运行时是否在线。
- 显示模型 API 是否已经配置。
- 统计手机本地保存的行程数量。
- 统计手机本地保存的图片数量。
- 本地数据不依赖网页持续打开，重新进入 App 后可以恢复。

#### 2.1.2 姿势教练

- 输入拍摄地点、人物数量、想要的动作和画面氛围。
- 未输入内容时，使用“大三巴前自然站立拍照”作为默认示例。
- 自动把需求带到“对话”页，生成姿势效果预览图。
- 生成图先保留在对话中，不会自动进入正式相册。
- 用户确认满意后，才可手动保存到本地相册。
- 支持对上一张效果图继续提出“往左、人物小一点、不要挡住建筑、换个姿势”等修改要求并重新生成。

#### 2.1.3 旅行记忆

- 保存饮食禁忌、兴趣、体力、步行时长、预算和其他旅行偏好。
- 旅行记忆保存在手机本地。
- 后续对话可以使用这份偏好作为个性化上下文。
- 本地旅行记忆上限为约 12000 字。

### 2.2 智能对话

“对话”页是 App 的智能交互中心，既支持普通问答，也承载图片、导航、导览和行程修改。

#### 2.2.1 普通旅行问答

- 使用 QwenPaw 主智能体回答澳门旅行问题。
- 可切换到设置中发现的 OpenAI-compatible 模型作为兼容回退。
- 支持 Markdown 正文、列表、链接和结构化内容展示。
- 首屏提供旅行启程、查看旅行技能、生成大三巴姿势预览图等快捷问题。
- 输入长度最多 10000 字。
- App 会展示主智能体当前的处理状态。

#### 2.2.2 模型选择与连接降级

- 模型下拉框可选择“QwenPaw 主智能体”或模型 API 返回的其他模型。
- QwenPaw 未配置时，提示进入设置页面。
- 回退模型未配置时，同样引导用户配置，而不会假装已经完成回答。
- 照片讲解和实时地图导航必须使用具备对应工具能力的 QwenPaw 服务。

#### 2.2.3 多会话与历史管理

- 新建普通对话。
- 切换历史对话。
- 删除历史对话。
- 每个会话单独记住所选模型。
- 第一个用户问题会自动成为会话标题。
- 历史抽屉按“行程文件夹”和普通对话整理。
- 每份行程可创建多个独立对话。
- 行程重命名后，对话文件夹标题同步更新。
- 删除行程后仍保留历史聊天，并标记为“已删除行程的历史对话”。
- 本地最多保留约 50 个会话；消息记录按本地策略保留最近约 200 条。

#### 2.2.4 对话与行程联动

- 对话可以绑定到某一份具体行程。
- 绑定后的智能体只能修改对应的行程 ID，避免误改其他行程。
- 智能体可返回结构化行程提案，包括标题、Markdown 行程、路线点、高德坐标和费用预算。
- 客户端在真正写入或修改行程前弹出确认框。
- 修改路线点时，同步更新与路线点相关联的费用项。
- 用户可以在同一行程文件夹中继续追问、修改路线、补充预算或新建对话。

#### 2.2.5 手机拍照与相册选图

- 点击图片按钮后可选择“拍照”或“从相册选择”。
- Android 原生拍照会调用手机相机，并把原图保存到系统相册。
- 从手机相册选择时沿用文件选择方式。
- 发送前显示图片预览，可移除后重新选择。
- 发送前必须描述想了解或想生成的内容，避免无意上传后自动处理。
- 图片会根据用途生成适合模型处理的分析版本，同时保留本地显示版本。

#### 2.2.6 照片处理模式

上传图片后可明确选择：

- **智能判断**：根据文字判断是讲解照片还是生成效果图；意图不明确时会先提问。
- **照片讲解**：识别建筑、文物、场景、文字或细节，并结合当前景点上下文讲解。
- **生成效果图**：把现场照片作为背景参考，只添加适合拍照的人物与姿势，尽量保持原建筑、视角、构图和光线。

识别证据不足、画面模糊或对象不唯一时，系统会提出具体澄清问题，而不是直接编造结论。

#### 2.2.7 图片生成与迭代

- 纯文字生成旅行姿势参考图。
- 基于现场照片合成拍照效果图。
- 基于上一张预览继续修改并重新生成。
- 支持 OpenAI Images 风格接口，以及项目中适配的 Qwen Image 多模态调用方式。
- 生成结果可能是 Base64 或 URL，客户端统一保存为本地可展示数据。
- 单张生成图片最大约 20 MB。
- 预览图默认只存在当前对话媒体库。
- 用户点击“保存到相册”后才成为本地旅行相册内容。

#### 2.2.8 实时地图导航

- 识别“怎么去大三巴”“帮我导航到澳门旅游塔”等自然语言。
- 自动判断公共交通、驾车或步行；未明确时默认公共交通。
- 只为本次导航请求临时读取手机位置。
- 通过后端地图能力查询目的地和路线。
- 展示目的地名称、地址、距离、预计时间和路线步骤。
- 公共交通最多展示 3 个方案，并标出推荐方案。
- 展示步行时间、步行距离、换乘次数、上车站、线路、下车站和途经站数。
- 若存在模拟巴士数据，可附加车辆预计到站时间、相距站数和车内拥挤度。
- 生成“打开高德地图开始导航”链接；Android 可调用外部地图 App。
- 明确说明位置仅用于本次查询，不把本次坐标写入对话内容。

#### 2.2.9 到达景点后的智能导览

- 行程开启后，根据定位判断是否到达已记录景点或临时选择的附近景点。
- 使用定位精度、新鲜度、景点半径和停留时间降低误触发。
- 在景点范围内持续停留约 20 秒后触发讲解。
- 已讲解过的景点不会因为区域重叠而连续重复触发。
- 自动生成约 300 至 500 字的现场历史文化故事，并结合可观察建筑细节。
- 讲解后提供 6 个追问方向：历史背景、文化故事、建筑特色、附近美食、拍照打卡点、下一站路线。
- 用户可以点击选项、回复编号或直接输入问题。
- “附近美食”和“拍照打卡点”可通过地图服务查询附近地点，展示距离、地址、分类、评分和地图链接。
- 手机定位过期时使用景点坐标并明确提示，不冒充实时定位。

#### 2.2.10 对话隐私边界

- 未开启“共享本地旅程”时，主 Agent 看不到手机行程。
- 未同步的本地相册照片对主 Agent、子 Agent 和 MCP 不可见。
- 用户在当前消息主动上传的照片，仅授权处理这一张照片。
- 对话上下文中的行程摘要是结构化副本，原始行程仍保存在手机。
- 内部控制块会在展示前过滤，不把内部协议直接显示给用户。

### 2.3 旅程

“旅程”页负责从规划到实地游览的完整生命周期。

#### 2.3.1 AI 规划新行程

- 选择 1 至 14 天。
- 设置旅行节奏。
- 填写出发区域。
- 填写历史、美食、亲子、拍照等兴趣。
- 补充同行人数、无障碍需求、预算和饮食偏好。
- AI 返回完整 Markdown 行程与结构化路线点。
- 路线点包含日期、时间、地点、交通或提醒、客流区域 ID 和可用坐标。
- 规划要求包含景点间交通、预计时间、预约提示、雨天替代和安全提醒。
- 不虚构实时人数、票价或营业状态。
- 手机本地最多保存约 30 份行程。

#### 2.3.2 行程选择与管理

- 从已保存行程中切换当前行程。
- 显示未开始、进行中、已结束状态。
- 重命名行程，并同步更新关联对话文件夹。
- 删除整份行程；删除前明确提示关联对话和费用的处理方式。
- 按“总览”或具体第几天查看路线。
- 显示路线点数量、日期、时间、地点、安排说明和当前进度。

#### 2.3.3 手工编辑路线

- 修改行程名称。
- 修改地点名称、所属天数、时间和安排说明。
- 添加新的路线点。
- 删除路线点。
- 支持编辑当前查看范围或整份行程。
- 地点名称改变后，尝试重新匹配客流区域、坐标和影响半径。
- 至少保留一个路线点，空名称不能保存。
- 路线修改后同步更新相关费用；失败时保留原行程。

#### 2.3.4 地图与路线展示

- 在地图上展示行程路线点。
- 按天切换路线视图。
- 显示当前选中范围内的站点数量和路线顺序。
- 可调用地图路线能力补充站点间线路。
- 规划、地图和列表使用同一份结构化路线点，避免只展示无法执行的 Markdown 文本。

#### 2.3.5 开始与结束行程

- 保存规划不会自动读取位置。
- 用户必须点击开始，并在用途说明后授权定位。
- 同一时间只允许一份行程处于进行中。
- 开始后由 Android 原生 GPS 持续读取位置；网页预览回退到 Web Geolocation。
- 结束后停止定位，保留行程和调整记录。
- Android 通知、App 弹窗和原生 TTS 可播报开始、到达、调整和结束状态。
- 蓝牙眼镜作为媒体输出设备时，TTS 可从眼镜播放；否则从手机扬声器播放。

#### 2.3.6 自由顺序与出发地选择

- 行程开启后先识别当前位置或附近酒店、地标。
- 展示附近可去景点及其客流信息。
- 用户可以选择下一站，也可以“先自由走走”。
- 导览不要求严格按原计划顺序，实际到达任一已记录或候选景点都可触发讲解。
- 选择目的地后，到达对应范围再自动讲解。

#### 2.3.7 实时客流与动态调整

- 从 `data_publish` 获取景点最新人数和拥挤度。
- GPS 的 WGS-84 坐标会转换为地图和发布器使用的 GCJ-02。
- 区分“0 人”“暂无数据”和“数据已过期”。
- 超过 30 分钟的读数显示为过期，不冒充实时数据。
- 可按最新客流重新排列当天尚未游览的景点。
- 不修改已经完成的部分和后续日期。
- 当天没有新鲜客流读数时保留原顺序。

### 2.4 账单

手机“账单”页按行程管理旅行预算，与酒店后端的行程费用接口联动。

#### 2.4.1 行程预算总览

- 选择一份本地行程查看对应账单。
- 默认优先选择正在进行的行程。
- 显示费用总额、费用数量、必要支出和可选支出。
- 金额以分为内部单位，界面按人民币元显示。
- 行程变化时自动刷新可选行程和费用数据。

#### 2.4.2 费用明细管理

- 添加费用。
- 编辑费用。
- 删除单笔费用。
- 刷新后端数据。
- 费用分类包括：住宿、门票、交通、餐饮、其他。
- 每笔费用可填写名称、地点、第几天、单价、数量、备注。
- 可标记为必要支出或可选支出。
- 数量大于 1 时显示单价乘数量。
- 删除后自动更新预算汇总。

#### 2.4.3 与行程的双向一致性

- 智能体生成行程时可以同时生成费用预算。
- 行程路线点名称或结构改变时，可同步更新相关费用项。
- 删除整份行程时，后端支持按行程 ID 批量删除费用。
- 单笔费用接口支持部分更新，未提交字段不会被意外清空。

### 2.5 本地旅行相册

#### 2.5.1 导入和本地保存

- 一次选择并导入多张图片。
- 图片保存在 Android WebView 应用私有 IndexedDB 中。
- 读取照片拍摄时间和 EXIF GPS（如果原图包含）。
- 未配置识图模型时仍可先保存，稍后补充 AI 标注。
- 清除 App 数据或卸载 App 会删除本地相册。

#### 2.5.2 AI 整理与识别

- 为照片生成地点、场景、说明、标签和画面文字。
- 识图模型可单独配置；留空时复用文字模型。
- 导入后自动标注。
- 批量整理所有未标注或失败的照片。
- 单张照片可重新识别。
- 识别请求失败时记录失败状态和错误原因，支持稍后重试。
- 地点可能来自照片 GPS，也可能来自 AI 推断，并显示来源与置信度。

#### 2.5.3 搜索、筛选和地图

- 按地点、场景、说明、标签或画面文字搜索。
- 自动生成常见地点快捷标签，并显示照片数量。
- 照片网格展示缩略图、地点、场景和标注状态。
- 地图模式按照片地点聚合展示。
- 点击地图区域后只显示该区域的照片。
- 地图服务只接收地图瓦片范围，不接收照片或 AI 标注。

#### 2.5.4 照片详情

- 查看大图。
- 查看识别地点。
- 查看定位来源：EXIF、AI 识别或无定位。
- 查看 AI 描述、标签、识别到的画面文字和错误信息。
- 重新识别。
- 上传云端并授权 Agent。
- 删除云端副本以撤销 Agent 访问。
- 删除手机本地副本。

#### 2.5.5 云端同步和删除语义

相册提供三种同步模式：

1. **关闭，仅保存在手机**：所有照片仅本地可见。
2. **只同步手动选择的照片**：只有点击云端按钮的照片授权给 Agent。
3. **新照片自动同步**：新导入照片自动上传并授权给 Agent。

需要特别注意：

- 删除手机本地副本不会自动删除已存在的云端副本。
- “删除云端副本”会撤销 Agent 访问，但仍保留手机本地照片。
- 未上传的照片对 QwenPaw、子 Agent 和 MCP 完全不可见。

### 2.6 设置

#### 2.6.1 QwenPaw 智能对话

- 配置 QwenPaw 服务地址。
- 配置主 Agent ID，默认示例为 `lensgo-travel-director`。
- 可为远程服务保存 Bearer 登录令牌。
- 保存并测试 QwenPaw 连接。
- USB 调试时可通过端口反向映射使用手机的 `127.0.0.1:18088`。

#### 2.6.2 文字与识图模型

- 配置 OpenAI-compatible API Base URL。
- 安全保存 API Key。
- 配置普通文字模型。
- 配置专用识图模型；留空时复用普通模型。
- 自定义 LensGo 助手系统提示词。
- 保存配置。
- 保存并测试模型连接。

#### 2.6.3 图片生成模型

- 可单独配置图片 API Base URL。
- 可单独保存图片 API Key。
- 未单独填写时复用文字模型地址和 Key。
- 配置图片模型名称。
- 自动兼容项目支持的 Qwen Image 与 OpenAI Images 风格调用。

#### 2.6.4 Agent 数据权限

- 开关“允许主 Agent 使用本地旅程”。
- 设置相册同步模式。
- 明确展示当前是仅本地、手动选择同步还是自动同步。
- 关闭同步不会删除手机照片，也不会自动删除以前的云端副本。

#### 2.6.5 实时客流服务

- 配置 `data_publish` API 地址。
- 为每台设备保存独立的只读 API Key。
- 保存并测试连接，返回景点数量和拥有最新人数的景点数量。
- 清除本设备保存的人流 API Key。
- 模拟器支持 `10.0.2.2`；真机使用电脑或服务器的可访问地址。

#### 2.6.6 本地安全

- API Key、令牌和客流 Key 保存在 Android 应用私有目录，不回传到 React 页面。
- 页面只显示“已保存”状态，不显示已有密钥明文。
- 输入框留空会保留原 Key，避免编辑其他字段时覆盖凭证。
- 卸载 App 会删除本地配置和本地数据。
- Release 构建只接受 HTTPS；Debug 构建可为本地联调使用 HTTP。

## 3. Android 原生与智能眼镜能力

### 3.1 Android 原生能力

- Tauri v2 WebView 独立安装包。
- Android 应用私有文件与配置存储。
- 原生 GPS 持续定位与定位事件回传。
- 低优先级前台服务，在临时切换相机或其他 App 时维持运行。
- 原生通知。
- 原生 TTS。
- 调用手机相机并把原图保存到系统相册。
- 调用外部高德地图导航。
- Android 后台仍受系统省电和后台时限限制；重新打开 App 后自动恢复运行时。

### 3.2 智能眼镜链路

- 接收眼镜文字问答事件。
- 接收音频并衔接 ASR。
- 把 Agent 回答切分后交给 TTS。
- 接收眼镜拍照上传。
- 接收视频上传和视频理解任务。
- 通过 WebSocket 传递实时事件。
- 将生成图、识别图和媒体事件同步到 LensGo 控制中心或关联渠道。
- 支持 Telegram 文字与图片镜像（需要 Bot 配置）。

### 3.3 LensGo 多 Agent 分工

- **Travel Director**：主协调者，理解旅行需求并编排其他能力。
- **Pose Coach**：给出身体动作、手部、视线、机位和安全建议，生成姿势图提示。
- **Memory Keeper**：维护用户偏好和旅行记忆。
- **Vision Curator**：理解、筛选和组织旅行影像。
- **Media Archivist**：归档照片、视频和生成媒体。
- 旅行规划 Skill、地图 MCP、AI Drive 等工具按任务被主 Agent 调用。

## 4. QwenPaw 桌面/网页控制台的全部页面

QwenPaw 控制台是完整的开发与运营后台。插件还能继续注册新的菜单和路由，因此下列项目是当前源码内置页面。

### 4.1 业务与交互页面

- **Chat**：多会话聊天、流式回答、工具调用结果、附件与上下文交互。
- **LensGo**：Bridge 状态、实时事件、设备、媒体、QwenPaw 与 Telegram 集成状态。
- **智能行程**：展示按天行程、路线点、类型、地图线路与行程统计。
- **旅行相册**：展示旅行图片时间线、上传和 AI Drive 相关内容。
- **Coding**：项目文件树、多标签代码编辑、保存、Markdown 预览、选区复制到聊天并附带文件行号上下文。
- **Inbox**：查看 Agent 任务、事件、执行轨迹和详情。

### 4.2 控制类页面

- **Channels**：管理 Agent 可使用的消息渠道及连接配置。
- **Sessions**：查看活动与历史会话、切换、清理或删除会话。
- **Cron Jobs**：创建和管理定时任务。
- **Heartbeat**：管理 Agent 的周期性主动检查或心跳任务。

### 4.3 单个 Agent 的能力配置

- **Workspace**：浏览 Agent 工作区文件、记忆文件和日记内容。
- **Skills**：查看、启用和管理当前 Agent Skills。
- **Tools**：查看和配置 Agent 可调用工具。
- **MCP**：通过表单或 JSON 配置 Streamable HTTP、SSE、Stdio 等 MCP 服务。
- **ACP**：配置 Agent Communication Protocol 相关能力。
- **Agent Config**：编辑当前 Agent 的身份、提示、运行行为和其他配置。
- **Agent Stats**：查看当前 Agent 的运行统计和使用情况。

### 4.4 全局设置页面

- **Agents**：创建、选择和管理 Agent。
- **Models**：管理远程模型、本地模型、自定义 Provider、模型协议、连接测试和模型选择。
- **Skill Pool**：浏览和管理可复用 Skill 池。
- **Environments**：管理不同运行环境和环境配置。
- **Security**：管理认证、允许免认证主机、文件保护、工具保护、安全规则、Skill 扫描、告警和白名单。
- **Token Usage**：查看 Token 消耗和模型使用统计。
- **Backups**：创建、查看和恢复配置或工作区备份。
- **Voice Transcription**：配置语音转写服务。
- **Debug**：查看和调整调试相关配置。
- **Plugin Manager**：查看、启用和管理插件。

### 4.5 桌面体验设置

- 简体中文、英文、日文、俄文、葡萄牙文等界面语言。
- 浅色、深色、跟随系统主题。
- 桌面关闭窗口行为：每次询问、隐藏或退出等配置。
- 桌面与网页共用主要控制台能力，Tauri 桌面端可补充原生能力。

## 5. 人流密度与巴士模拟数据发布器网页

发布器页面当前包含 8 个标签：**地图发布、巴士报站、批量发布、当前数据、发布历史、新增地点、部门管理、API 说明**。其中“部门管理”仅向有管理权限的账号显示。

### 5.1 登录、角色与权限

- 用户名和密码登录。
- 查看当前账号显示名称和角色。
- 退出登录。
- 角色包括 viewer、publisher、reviewer、admin。
- 程序接入使用独立 API Key 与 scope。
- 支持 `crowd:read`、`crowd:publish`、`crowd:regions:write`、`crowd:batches:revert`、`crowd:admin` 权限。
- 只读 Key 不能调用高德代理，避免消耗服务端高德额度。
- 业务接口要求网页登录会话或 Bearer API Key；健康检查与登录入口除外。

### 5.2 地图发布

- 选择城市和地点层级。
- 按关键词通过服务端代理搜索高德 POI。
- 在地图上点击任意位置并进行逆地理编码。
- 展示附近真实 POI 候选。
- 搜索结果点击后地图定位到对应位置。
- 已有数据地点以拥挤度颜色显示在地图上。
- 点击已有点可直接更新人数。
- 在侧面板编辑地点名称、层级、人数和拥挤度。
- 发布当前地点的人数。
- 刷新地图上的已有数据。
- 同一高德 POI 按 POI ID 去重。
- 纯坐标点按同城、同名和约 30 米范围去重。
- 搜索到新城市时可按高德行政区编码自动登记城市。

### 5.3 巴士报站

- 选择演示路线。
- 查看路线摘要和有序站点。
- 填写车辆编号。
- 选择当前所在站。
- 设置驶向下一站的进度百分比。
- 设置运行状态。
- 设置车内拥挤度。
- 设置延误分钟数。
- 设置行驶速度。
- 手动发布一次车辆状态。
- 按指定秒数自动连续发布。
- 配置每站模拟用时。
- 暂停自动模拟。
- 停止车辆。
- 查看当前车辆、位置、下一站、预计到站和报站结果。
- 到站时间由路线分钟数、车辆进度和延误量计算。
- 内置 4 条澳门演示路线。
- 所有巴士接口明确返回 `source: mock` 和免责声明，不能当作真实交通数据。

### 5.4 批量发布

- 按城市、关键词和层级搜索本地已有区域。
- 搜索中文名、英文名、别名和 region ID。
- 筛选区、街道或景点。
- 添加单个结果到发布清单。
- 一次添加当前全部搜索结果。
- 为每个地点填写人数和拥挤度。
- 按不同层级生成量级合理的随机人数。
- 从清单移除项目或清空清单。
- 填写发布者、观测时间和备注。
- 一次性原子发布整批数据。
- 每次发布生成唯一批次 ID。

### 5.5 当前数据

- 按城市筛选。
- 按区域层级筛选。
- 按是否包含无读数地点筛选。
- 查看地点、人数、拥挤度、观测时间和发布者。
- 区分 0 人与没有数据。
- 标记超过时限的过期数据。
- 手动刷新。

### 5.6 发布历史

- 查看发布批次列表。
- 查看批次的发布者、时间、数量、备注和状态。
- 查看批次明细。
- 按权限撤销整个批次。
- 批量发布采用事务，要么全部写入，要么全部失败。

### 5.7 新增地点

- 新增区、街道或景点。
- 选择城市和上级区域。
- 填写中文名、英文名和别名。
- 填写经纬度。
- 指定输入坐标系。
- WGS-84 坐标在服务端转换为 GCJ-02。
- 设置可选影响半径。
- 使用父级关系建立“景点 → 街道 → 区”三级层级。

### 5.8 部门管理

- 管理员查看部门账号列表。
- 创建账号，设置用户名、显示名称、初始密码和角色。
- 更新账号状态或角色。
- 查看软件 API Key 列表。
- 创建独立 API Key，设置名称、到期时间和 scope。
- 完整 Key 只显示一次。
- 一键复制刚创建的 Key。
- 撤销 API Key。
- 查看账号、Key、发布和撤销相关审计记录。
- 配置审计记录显示条数并刷新。

### 5.9 API 说明

- 网页展示主要 HTTP 接口表。
- 提供调用示例。
- 展示服务状态和当前数据统计。
- API 数据坐标统一为 GCJ-02。
- `reading: null` 明确表示没有数据。

### 5.10 人流与巴士 API

| 方法 | 路径 | 功能 |
|---|---|---|
| GET | `/api/health` | 最小健康状态 |
| POST | `/api/login` | 网页账号登录 |
| POST | `/api/logout` | 退出登录 |
| GET | `/api/me` | 当前账号与权限 |
| GET | `/api/meta` | 层级、拥挤度阈值和权限元数据 |
| GET/POST | `/api/cities` | 搜索或新增城市 |
| GET/POST | `/api/regions` | 搜索或新增区、街道、景点 |
| GET | `/api/regions/{id}` | 区域详情 |
| GET | `/api/regions/{id}/history` | 单区域历史读数 |
| GET | `/api/density/latest` | 最新人流读数 |
| GET | `/api/amap/status` | 高德配置状态 |
| GET | `/api/amap/search` | 高德 POI 搜索代理 |
| GET | `/api/amap/around` | 高德周边搜索代理 |
| GET | `/api/amap/regeo` | 高德逆地理编码代理 |
| POST | `/api/regions/from-amap` | 幂等登记高德地点 |
| POST | `/api/readings` | 原子发布一批人流数据 |
| GET | `/api/bus/routes` | 巴士路线与站点 |
| GET/POST | `/api/bus/vehicles` | 查询或发布车辆状态 |
| GET | `/api/bus/stops/{id}/arrivals` | 指定站点到站预测 |
| GET | `/api/batches` | 发布批次列表 |
| GET/DELETE | `/api/batches/{id}` | 批次明细或撤销批次 |
| GET/POST/PATCH | `/api/admin/users` | 管理部门账号 |
| GET/POST/DELETE | `/api/admin/api-keys` | 管理程序 API Key |
| GET | `/api/admin/audit` | 查询审计记录 |

## 6. 酒店预订、账单与酒店工作台网页

酒店网页名为“旅屿”，同时适配桌面导航和移动端底部导航，包含 **找酒店、账单、酒店工作台** 三个主视图。

### 6.1 找酒店

- 输入目的地。
- 选择入住和离店日期。
- 校验离店日期必须晚于入住日期。
- 选择 1 至 4 位住客。
- 搜索指定日期内的可售库存。
- 展示酒店名称、区域、星级、评分和概念图。
- 展示本次入住总价、房型、设施、描述和剩余房量。
- 打开酒店详情与预订确认弹窗。
- 展示入住日期、离店日期、房型、房费和服务费。
- 创建预订后锁定房间约 10 分钟。
- 自动生成待付款账单并跳转到账单中心。
- 页面说明 AI 可搜索、比较和整理账单，但不能默认替用户付款。

### 6.2 我的账单

- 显示待支付总额和待处理账单数量。
- 展示全部、待支付、已支付筛选入口。
- 选择或取消选择单笔待付款账单。
- 全选或取消全选待付款账单。
- 展示账单标题、说明、状态、版本和费用拆分。
- 查看账单时间线入口。
- 调整账单中的早餐服务。
- 调整前计算新的总价。
- 确认调整后生成新账单版本。
- 金额或版本变化会使以前的 AI 付款授权失效。
- 用户可直接完成模拟付款。
- 用户可请求 AI 一次性付款授权。

### 6.3 AI 一次性付款授权

- 授权请求只绑定用户选中的具体账单。
- 设置最高金额。
- 记录每一笔账单的版本。
- 用户可以拒绝或授予。
- Agent 不能批准或撤销自己的授权。
- 授权有效期 5 分钟。
- 授权只能使用一次。
- 账单集合、金额、版本或有效期不匹配时拒绝付款。
- 使用成功后授权状态变为已使用。
- 付款后账单变为已支付，预订变为已确认。
- 当前付款是模拟支付，不发生真实资金扣款。

### 6.4 酒店工作台

- 展示酒店运营统计。
- 选择或加载当前酒店。
- 查看逐日房型、价格、可售房数量和库存状态。
- 通过加减按钮调整可售房数量。
- 直接编辑每日房价。
- 修改后立即保存到后端。
- 保存失败时回滚界面到原值。
- 手机和桌面都可进入工作台。

### 6.5 活动记录

- 展示预订、账单调整、付款授权、安全操作和付款结果。
- 标记事件类型和发生时间。
- 用于向用户说明 AI 或用户刚刚执行了什么操作。

### 6.6 酒店 API

| 方法 | 路径 | 功能 |
|---|---|---|
| GET | `/api/v1/state` | 一次加载酒店、账单、授权、活动和用户状态 |
| GET | `/api/v1/hotels/search` | 按日期搜索可售酒店 |
| POST | `/api/v1/quotes` | 生成 1 至 30 晚的限时报价 |
| POST | `/api/v1/bookings` | 使用有效报价或当前库存创建预订与账单 |
| GET/POST | `/api/v1/bills` | 查询、创建或调整账单 |
| GET | `/api/v1/bills/{id}` | 查询单笔账单 |
| POST | `/api/v1/bills/{id}/adjustments/preview` | 预览账单调整 |
| POST | `/api/v1/bills/{id}/adjustments/confirm` | 确认调整并生成新版本 |
| GET/POST | `/api/v1/payment-authorizations` | 查询、请求、批准或撤销 AI 付款授权 |
| POST | `/api/v1/payment-sessions` | 用户或获授权 AI 发起模拟付款 |
| GET/PUT | `/api/v1/inventory` | 查询或更新每日价格库存 |
| GET | `/api/v1/attractions` | 查询演示景点数据 |
| GET | `/api/v1/openapi` | 查看机器可读接口说明 |
| GET/POST/DELETE | `/api/v1/trip-expenses` | 查询、新建或按行程删除预算项 |
| PATCH/DELETE | `/api/v1/trip-expenses/{id}` | 编辑或删除单笔预算项 |

## 7. 主要后端业务能力

### 7.1 QwenPaw 智能体后端

- 多 Provider 和多模型管理。
- 流式消息与多会话。
- Agent 创建、选择、状态和统计。
- Skills、Tools、MCP、ACP 和插件扩展。
- 工作区文件、记忆和上下文。
- 定时任务、心跳和主动执行。
- 渠道接入。
- Token 统计。
- 安全策略、工具保护、文件保护和 Skill 扫描。
- 备份、环境和运行配置。
- 旅行规划、附近地点、地图路线和相册接口。

### 7.2 人流后端

- SQLite 持久化城市、区域、读数、批次、巴士、账号、Key 和审计数据。
- 区、街道、景点三级层级。
- 人数与 0 至 4 级拥挤度。
- 按区域层级使用不同人数阈值自动推导拥挤度。
- 观测时间、发布时间和数据来源分离。
- 批次事务和整批撤销。
- 高德 Key 只留在服务端。
- GCJ-02 坐标统一和 WGS-84 转换。

### 7.3 酒店后端

- D1/SQLite 风格结构化存储。
- 酒店与逐日库存。
- 跨日期库存汇总。
- 限时报价。
- 预订和库存锁定。
- 账单拆分、版本和调整。
- 行程预算项。
- 用户与服务身份区分。
- AI 一次性、限额、限账单版本付款授权。
- 模拟付款和活动审计。

## 8. 数据、隐私与安全边界

- 手机行程、旅行记忆和本地相册默认只保存在手机。
- 未获授权的本地照片不会发送给 Agent。
- API Key 和登录令牌不应写入 Git、文档、截图或日志。
- 仓库通过 `.gitignore` 排除 `.env`、凭证、私钥、数据库、运行日志、上传数据、依赖和构建产物。
- 客流发布器通过账号角色、API Key scope、会话来源检查和审计记录控制访问。
- 高德服务 Key 由后端代理保管，不下发给只读客户端。
- 酒店系统的 AI 付款默认关闭，且 Agent 不能自行授权。
- 当前酒店付款、巴士位置和部分人流数据用于产品演示；界面和 API 都应保持明确说明。

## 9. 依赖外部配置的功能

以下功能已有代码，但必须提供对应配置才能真正运行：

- QwenPaw 主 Agent：需要可访问的 QwenPaw 地址和 Agent ID。
- 普通对话回退：需要 OpenAI-compatible 文字模型地址、Key 和模型名。
- 图片识别：需要支持视觉输入的模型。
- 图片生成：需要图片模型地址、Key 和模型名。
- 高德地点搜索、附近查询和路线规划：需要有效的高德 Web 服务 Key。
- 云端相册：需要 AI Drive 或项目配置的文件服务。
- Telegram：需要 Bot Token 和目标 Chat ID。
- 智能眼镜语音识别：需要有效的阿里云 NLS AppKey 与 RAM 凭证。
- 真机连接局域网服务：手机和服务端必须网络互通，或使用 HTTPS 公网部署。

## 10. 已知产品边界

- Android 省电策略可能停止后台服务，重新打开 App 后恢复。
- 本地数据尚未提供完整的用户导出、导入和加密备份界面。
- 人流发布器当前以单机 SQLite 为主，适合本地或小规模演示。
- 人流区域当前主要使用中心点和影响半径，完整区域多边形仍可扩展。
- 区级人数不会自动由所有下属街道、景点汇总。
- 模拟巴士不是交通事务局实时数据。
- 酒店付款为模拟付款，不连接真实银行卡或支付渠道。
- 不同图片供应商若不兼容当前接口，需要增加专用适配器。
- 正式公网发布应使用 HTTPS/WSS、强认证、最小权限 Key 和 Android 正式签名。

## 11. 典型完整使用流程

### 11.1 游客规划并开始旅行

1. 在设置中连接 QwenPaw、模型和客流服务。
2. 在 LensGo 首页保存旅行偏好。
3. 在“旅程”中填写天数、区域、兴趣和限制并生成行程。
4. 在“账单”中补充住宿、门票、交通和餐饮预算。
5. 点击开始行程并授权定位。
6. 选择附近下一站或自由行走。
7. 到达景点后收到通知、TTS 和自动导览。
8. 查询附近美食、拍照点、下一站路线和实时客流。
9. 必要时按客流重排当天未游览景点。
10. 完成后结束行程，停止定位并保留记录。

### 11.2 现场拍照与姿势效果图

1. 在对话中调用相机或选择相册照片。
2. 选择“照片讲解”了解建筑或文物，或选择“生成效果图”。
3. 查看效果预览。
4. 用文字继续调整人物位置、姿势和构图。
5. 满意后保存到本地相册。
6. 按隐私策略决定是否上传云端并授权 Agent。
7. 随时删除云端副本以撤销访问。

### 11.3 后台发布客流并在 App 使用

1. 发布人员登录数据发布器。
2. 在地图上搜索或点击一个景点。
3. 填写人数和拥挤度并发布。
4. App 使用只读 Key 拉取最新数据。
5. 旅程页展示人数、拥挤度、新鲜度和过期状态。
6. 用户确认后按客流调整尚未游览的路线点。

### 11.4 酒店预订与受控 AI 付款

1. 游客选择日期和住客人数并搜索酒店。
2. 查看完整总价、剩余库存和设施。
3. 创建预订并生成待付款账单。
4. 调整早餐等附加服务，账单生成新版本。
5. 用户直接模拟付款；或者请求一次性 AI 授权。
6. 用户确认账单、限额和有效期后授予权限。
7. AI 只能对原账单集合和版本付款一次。
8. 付款完成后订单确认，并写入活动记录。

## 12. 代码位置速查

| 模块 | 主要目录 |
|---|---|
| 手机 App | `project_front/middle_stage/Qwen_competion/qwen_compitition/console/src/mobile-local/` |
| QwenPaw 控制台 | `project_backend/middle_stage/Qwen_competion/qwen_compitition/console/src/` |
| QwenPaw 后端 | `project_backend/middle_stage/Qwen_competion/qwen_compitition/src/qwenpaw/` |
| LensGo 眼镜服务 | `project_backend/middle_stage/Qwen_competion/lensgo-macao/` |
| 人流与巴士发布器 | `project_backend/middle_stage/data_publish/` |
| 酒店与账单系统 | `project_backend/middle_stage/hotel_book/` |
| 手机前端交付副本 | `project_front/middle_stage/` |
| 项目启动说明 | `02_本地启动指南.md` |
| 服务器部署说明 | `03_服务器迁移部署指南.md` |

---

这份说明描述的是当前仓库中的整体能力。实际演示时，建议同时打开 Android App、QwenPaw 控制台、人流发布器和酒店网页，分别展示游客端体验、智能体执行过程、实时模拟数据发布以及受控付款与酒店运营工作台。
