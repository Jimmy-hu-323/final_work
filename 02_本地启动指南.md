# 本地启动指南（Windows 前后端）

> 最后实测：2026-08-20  
> 正确项目根目录：`Z:\qwen_compitition\final_work`  
> 本文默认使用 PowerShell。

## 1. 你会启动哪些服务

这不是传统的“一个前端 + 一个后端”，而是三个可运行模块：

| 模块 | 前端 | 后端/数据 | 默认端口 |
|---|---|---|---:|
| `data_publish` | 原生 HTML/JS + Leaflet | Python 标准库 HTTP + SQLite | `18099` |
| `hotel_book` | React/Vinext | Vinext API + Cloudflare D1 本地绑定 | `18110` |
| `Qwen_competion` | Qwen Console / Tauri | QwenPaw + LensGo | `18088`、`18765`、`18000` |

`project_front` 只是前端维护镜像，不能替代上述完整模块。日常启动统一从 `project_backend\middle_stage` 进行。

## 2. 当前机器的环境结论

2026-08-20 已核验：

| 工具 | 当前状态 | 项目要求/说明 |
|---|---|---|
| Windows Z 盘 | 可访问 | 必须能看到 `Z:\qwen_compitition\final_work` |
| Python | 3.12 可用 | QwenPaw 使用 3.11～3.13；人流服务只用标准库 |
| Qwen `.venv` | Python 3.12.7，可用 | editable 安装已重绑当前源码 |
| Node.js | 22.16 | 酒店和 Qwen Console 建议 `>=22.13` |
| npm | 10.9 | 可用 |
| Git | 2.54 | 可用 |
| Rust/Cargo/JDK | 可用 | 仅 Tauri/Android 构建需要 |
| pytest | 当前 `.venv` 未安装 | 只影响完整 pytest 测试，不影响启动 |
| Docker/WSL/adb | 当前不可用或未配置 | 不影响本地 Web；手机/容器流程另行安装 |

当前 `doctor` 是 0 个错误。AMap MCP、AI Drive MCP 和 pytest 依赖是警告，不是三个核心 Web 服务的启动阻断项。

## 3. 启动前 30 秒检查

打开 PowerShell：

```powershell
Test-Path -LiteralPath 'Z:\qwen_compitition\final_work'

Get-NetTCPConnection `
  -LocalPort 18000,18088,18099,18110,18765 `
  -State Listen `
  -ErrorAction SilentlyContinue
```

第一条应返回 `True`。第二条没有输出表示端口空闲；如有输出，先确认是否已经启动过本项目，不要直接杀未知进程。

然后诊断集成主系统：

```powershell
Set-Location 'Z:\qwen_compitition\final_work\project_backend\middle_stage\Qwen_competion'
& .\.venv\Scripts\python.exe .\scripts\integrated.py doctor
```

末尾应为：

```text
Doctor completed: 0 error(s).
```

## 4. 当前项目的最快启动方式

推荐使用三个 PowerShell 窗口，按“人流 → 酒店 → Qwen/LensGo”顺序启动。这样主 Agent 启动后就能连接两个业务服务。

### 窗口 A：人流发布器

```powershell
Set-Location 'Z:\qwen_compitition\final_work\project_backend\middle_stage\data_publish'
python run.py --host 127.0.0.1 --port 18099
```

打开：

```text
http://127.0.0.1:18099/
```

健康检查：

```powershell
Invoke-RestMethod 'http://127.0.0.1:18099/api/health'
```

应返回 `status = ok`。前端由同一个 Python 服务托管，不要双击 `web\index.html`。

### 窗口 B：酒店前后端

```powershell
Set-Location 'Z:\qwen_compitition\final_work\project_backend\middle_stage\hotel_book'
npm.cmd run dev -- --port 18110 --hostname 127.0.0.1
```

使用 `npm.cmd` 和长参数可避免某些 PowerShell/npm 组合把 `-H` 误解析成帮助选项。

打开：

```text
http://127.0.0.1:18110/
```

检查页面、路由和 D1：

```powershell
(Invoke-WebRequest -UseBasicParsing 'http://127.0.0.1:18110/').StatusCode
(Invoke-WebRequest -UseBasicParsing 'http://127.0.0.1:18110/api/v1/openapi').StatusCode
(Invoke-WebRequest -UseBasicParsing 'http://127.0.0.1:18110/api/v1/state').StatusCode
```

三项都应为 `200`。

### 窗口 C：QwenPaw + LensGo

```powershell
Set-Location 'Z:\qwen_compitition\final_work\project_backend\middle_stage\Qwen_competion'
& .\.venv\Scripts\python.exe .\scripts\integrated.py start
```

等价简写：

```powershell
.\scripts\start.ps1
```

该命令启动：

- QwenPaw/Console：`http://127.0.0.1:18088/`
- LensGo WebSocket：`ws://127.0.0.1:18765/chat`
- LensGo HTTP/Bridge：`http://127.0.0.1:18000/`

QwenPaw 检查：

```powershell
Invoke-RestMethod 'http://127.0.0.1:18088/api/version'
```

当前实测版本是 `2.0.0.post3`。LensGo WebSocket 端口检查：

```powershell
Test-NetConnection 127.0.0.1 -Port 18765
```

Bridge 状态接口需要 `.env.integrated` 中的 Bridge Token。不要把 Token 粘贴到共享终端记录或文档；用本机安全变量发出请求即可。

> 完整 `start` 会初始化当前配置中启用的外部通道。只想安全验证 Qwen Web 服务、不连接 LensGo/Telegram 时，先用 `start --qwen-only`。

## 5. 分模块启动 Qwen/LensGo

只启动 QwenPaw：

```powershell
& .\.venv\Scripts\python.exe .\scripts\integrated.py start --qwen-only
```

只启动 LensGo（要求 QwenPaw 已在 `18088` 运行）：

```powershell
& .\.venv\Scripts\python.exe .\scripts\integrated.py start --glasses-only
```

桌面 App：

```powershell
.\scripts\app.ps1
```

或双击 `启动 LensGo App.vbs`。桌面 App 需要已构建的 Console 和 `pywebview`；启动器会从 TOML 读取 `18765/18000`，不再使用旧端口。

## 6. 全新机器初始化

不要覆盖已有机器的 `.env`、`.dev.vars`、数据库或 `workspace`。以下步骤只用于全新副本。

### 6.1 基础工具

安装并确认：

```powershell
python --version
node --version
npm --version
git --version
```

建议：Python 3.12、Node 22 LTS 或更高的兼容版本。

### 6.2 初始化人流服务

```powershell
Set-Location 'Z:\qwen_compitition\final_work\project_backend\middle_stage\data_publish'
Copy-Item .env.example .env
```

编辑 `.env`，至少为认证 pepper 设置高强度随机值。不要把值提交。创建管理员会交互式读取密码：

```powershell
python run.py --create-admin admin --admin-display-name 'Administrator' --seed-only
```

然后启动：

```powershell
python run.py --host 127.0.0.1 --port 18099
```

该服务只依赖 Python 标准库，无需 `pip install`。

### 6.3 初始化酒店服务

```powershell
Set-Location 'Z:\qwen_compitition\final_work\project_backend\middle_stage\hotel_book'
npm.cmd ci
Copy-Item .dev.vars.example .dev.vars
```

编辑 `.dev.vars` 并设置至少 16 字符的内部服务 Token。不要提交。然后：

```powershell
npm.cmd run dev -- --port 18110 --hostname 127.0.0.1
```

本地 D1 状态由 Vinext/Cloudflare 开发运行时创建。必须从 `hotel_book` 正确目录启动，否则会出现 `D1 database binding DB is unavailable`。

### 6.4 初始化 QwenPaw/LensGo

```powershell
Set-Location 'Z:\qwen_compitition\final_work\project_backend\middle_stage\Qwen_competion'
Copy-Item .env.integrated.example .env.integrated
```

只在 `.env.integrated` 中填写所需 Provider/模型、Bridge Token、酒店和人流地址，以及你实际启用的可选外部服务。不要在文档或聊天中展示值。

执行一次完整初始化和 Console 构建：

```powershell
.\scripts\bootstrap.ps1 --build-console
```

该命令会：

1. 建立 `.venv`；
2. editable 安装当前 QwenPaw 和 Glasses 源码；
3. 初始化统一 QwenPaw 工作区和五个 LensGo Agent；
4. 安装可用的 MCP Driver；
5. `npm ci` 并构建 Console；
6. 在项目搬迁后自动把持久化 Agent 路径重基到当前目录。

完成后：

```powershell
& .\.venv\Scripts\python.exe .\scripts\integrated.py doctor
& .\.venv\Scripts\python.exe .\scripts\integrated.py start
```

## 7. 前端开发模式

### Qwen Console

先保持 QwenPaw 在 `18088` 运行，然后开新窗口：

```powershell
Set-Location 'Z:\qwen_compitition\final_work\project_backend\middle_stage\Qwen_competion\qwen_compitition\console'
npm.cmd run dev
```

Vite 默认地址是 `http://127.0.0.1:5173`，`/api` 默认代理到 `http://127.0.0.1:18088`。如后端另有地址：

```powershell
$env:VITE_DEV_PROXY_TARGET = 'http://127.0.0.1:18088'
npm.cmd run dev
```

生产构建：

```powershell
npm.cmd run build
```

当前构建可通过，但会出现已有的大 chunk 和循环 chunk 警告；这是性能优化项，不是构建失败。

### 酒店前端

酒店页面和 API 在同一个 Vinext 进程中，使用前述 `npm.cmd run dev` 即可。不要从 `project_front\hotel_book` 尝试完整联调，因为镜像没有完整 API/数据层。

### 人流前端

人流页面和 API 必须同源，改完 `web\` 后刷新 `18099` 页面。不要用 `file://` 打开页面。

## 8. 生产模式本地预演

酒店：

```powershell
Set-Location 'Z:\qwen_compitition\final_work\project_backend\middle_stage\hotel_book'
npm.cmd run build
npm.cmd run start -- --port 18110 --hostname 127.0.0.1
```

Qwen Console 构建后由 QwenPaw `18088` 同源托管，无需另启前端服务器。

## 9. 停止服务

三个窗口分别按 `Ctrl+C`：

- `data_publish` 停止 Python HTTP 服务；
- `hotel_book` 停止 Vinext/Vite；
- `integrated.py start` 会清理 QwenPaw 和 LensGo 两个子进程。

`Ctrl+C` 后命令行可能显示退出码 1，这是 Windows 终端中断的表现；确认端口已释放即可：

```powershell
Get-NetTCPConnection `
  -LocalPort 18000,18088,18099,18110,18765 `
  -State Listen `
  -ErrorAction SilentlyContinue
```

## 10. 构建与测试

### 已实测的核心回归

```powershell
# Qwen 集成布局：当前 10/10 通过
Set-Location 'Z:\qwen_compitition\final_work\project_backend\middle_stage\Qwen_competion'
& .\.venv\Scripts\python.exe -m unittest discover -s tests -p test_integrated_layout.py -v

# 酒店：包含完整 Vinext 构建与 HTML 回归
Set-Location 'Z:\qwen_compitition\final_work\project_backend\middle_stage\hotel_book'
npm.cmd run test

# 人流前端静态测试
Set-Location 'Z:\qwen_compitition\final_work\project_backend\middle_stage\data_publish'
node .\web\tests\auth_ui_static_test.js
```

完整 Python 测试：

```powershell
Set-Location 'Z:\qwen_compitition\final_work\project_backend\middle_stage\Qwen_competion'
& .\.venv\Scripts\python.exe .\scripts\integrated.py test
```

当前 `.venv` 缺 `pytest`/`pytest-asyncio`。请只在证书正常的安全网络中安装，不要用跳过 TLS 验证的参数：

```powershell
& .\.venv\Scripts\python.exe -m pip install pytest pytest-asyncio
```

## 11. 常见故障

### `Z:` 找不到

映射盘可能只在当前登录用户会话中存在，管理员 PowerShell、计划任务或服务账户看不到。先运行：

```powershell
Get-PSDrive Z
Test-Path 'Z:\qwen_compitition\final_work'
```

服务部署不要依赖映射盘，应迁移到本机物理路径或 Linux `/srv`。

### 移动项目后 `import qwenpaw` / `import glasses` 失败

editable 安装仍指旧路径。最稳妥的处理：

```powershell
Set-Location 'Z:\qwen_compitition\final_work\project_backend\middle_stage\Qwen_competion'
.\scripts\bootstrap.ps1
```

然后重新运行 `doctor`。不要手改虚拟环境中的多个 `.pth` 文件。

### 日志中的 Agent 工作区仍是旧路径

运行：

```powershell
& .\.venv\Scripts\python.exe .\scripts\integrated.py bootstrap --skip-install
& .\.venv\Scripts\python.exe .\scripts\integrated.py doctor
```

当前版本会自动重基 `config.json` 和各 `agent.json` 的 `workspace_dir`。

### 酒店命令只打印 npm 帮助

使用：

```powershell
npm.cmd run dev -- --port 18110 --hostname 127.0.0.1
```

不要使用容易被误解析的短 `-H` 组合。

### 酒店提示 D1 绑定不可用

确认当前目录是完整的 `project_backend\middle_stage\hotel_book`，且使用 `npm run dev/start`；不要从 `project_front` 或只打开静态文件。

### `18000` 和旧文档 `18866` 不一致

一体化运行只认：

```text
Qwen_competion/config/lensgo.integrated.toml
Qwen_competion/scripts/integrated.py
```

当前 HTTP/Bridge 是 `18000`。不要混用 `lensgo-macao` 的旧 `run_all.sh`。

### AMap MCP 警告

QwenPaw 的 AMap MCP 与 `data_publish` 的高德 Web Key 是两套配置。未部署外部 AMap MCP 时保持 Driver 禁用即可；不影响基础页面、酒店和人流服务启动。

### Android 构建路径

移动端脚本当前默认把 SDK/cache 放在 `D:\Android_studio\LensGoCache`。本机该路径存在；其他机器请给脚本传 `-CacheRoot`，并给 Android Studio 启动器传正确的 `-AndroidStudioExe`。这不是 Web 启动前置条件。

## 12. 启动成功判定

以下均满足，即可认为本地核心系统启动正确：

- `doctor` 为 0 error；
- `18099/api/health` 返回 200；
- `18110/`、`18110/api/v1/state` 返回 200；
- `18088/api/version` 返回 200；
- `Test-NetConnection 127.0.0.1 -Port 18765` 成功；
- LensGo 使用 `18000`，没有误连 `18866`；
- Qwen 启动日志中的所有 Agent 路径都包含 `final_work`。

